# AI Provider: "mock" or "claude"
AI_PROVIDER=mock

# Anthropic API Key (required when AI_PROVIDER=claude)
ANTHROPIC_API_KEY=

# Server port
PORT=3001

# Workspace root directory (relative to server root)
WORKSPACE_ROOT=../../workspace
